title: Mosquito
url: "https://mosquito-cr.github.io" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
exclude:
 - Gemfile
 - Gemfile.lock

include:
  - robots.txt

collections:
  manual:
    output: true

plugins:
  - jekyll-toc
